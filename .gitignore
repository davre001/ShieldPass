# Dependencies
node_modules/
npm-debug.log

# Rust / Cargo / Soroban / Noir build artifacts
target/
*.rs.bk
Prover.toml
Verifier.toml

# Environment Variables — NEVER commit secrets
.env
.env.*
!.env.example
*.bak

# Database (SQLite) — Prisma writes the dev db under backend/prisma/
backend/prisma/dev.db
backend/prisma/dev.db-journal

# Compiled TypeScript
backend/dist/
dist/

# IDE / OS
.DS_Store
.vscode/
.idea/
Thumbs.db

# Test Files
test-frontend.html

contract_details.md
frontend-tester
docs


# Prisma 7 generated client (regenerated at build via prisma generate)
backend/src/generated/
hh

# Circom build workspace — intermediates (ptau, zkeys, wasm); runtime copies live in backend/circuits + frontend/public
SDK/circom/build/
*.ptau
