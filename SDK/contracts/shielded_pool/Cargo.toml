[package]
name = "shielded-pool"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = "25.0.0"

[dev-dependencies]
# Enable testutils for writing unit tests with mock environments
soroban-sdk = { version = "25.0.0", features = ["testutils"] }

[lib]
crate-type = ["cdylib", "rlib"]
